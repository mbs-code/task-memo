{
  "private": true,
  "name": "task-memo",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "tauri": "tauri"
  },
  "dependencies": {
    "@tauri-apps/api": "^1.0.1",
    "nuxt": "3.0.0-rc.4"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^1.0.0"
  }
}
